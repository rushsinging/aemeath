<!-- Migrated from: docs/bug/archived/035-write-tool-worktree-path.md -->
# Bug #35: Write tool 在 worktree 中写入错误分支

**状态**：已修复并确认（2026-05-18）
**优先级**：高
**发现日期**：2026-05

## 症状

使用 git worktree 隔离开发时（如 `.worktrees/feature-xxx/`），LLM 通过 Write/Read 等文件工具使用相对路径，文件实际写入 main worktree 而非当前 worktree。主 agent 和 sub-agent 都受影响。

## 根因

文件工具的路径解析基于进程 CWD（main worktree 根目录），未感知用户当前操作的 worktree 上下文。主 agent 启动时 CWD 为 main worktree，sub-agent 也是同一进程空间，因此所有文件工具都解析到 main worktree。

## 修复方案

新增 `ToolContext.path_base` 作为相对路径解析基准，并由 Bash 工具在命令结束后记录实际 `$PWD`，让后续 Read/Edit/Write/Glob/Grep/Bash 相对路径基于当前 worktree/目录解析，同时仍用 `cwd` 作为安全边界。

新增回归测试覆盖 Bash `cd` 后路径基准持久化，以及路径工具按当前 worktree base 解析相对路径。

## 验证

- `cargo test -p aemeath-tools` 通过（24 passed）
- `cargo check` 通过

## 修复 Commit

- `ace91eb` — fix(tools): persist worktree path base refs #35
- `e98242f` — Merge branch 'bug/35-write-tool-worktree' refs #35

## 涉及路径

- `aemeath-tools/src/` — ToolContext、BashTool 及文件工具路径解析逻辑
