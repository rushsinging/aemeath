#!/usr/bin/env bash
# This script was removed during the workspace slim refactor (91e0424).
# Kept as a no-op stub so cached hook configs don't block session end.
echo "check-openapi-sdk: skipped (package removed)"
exit 0
