# Code Review Report — 未提交改动

**审查日期**: 2026-04-06  
**最后更新**: 2026-04-06  
**改动范围**: 52 个文件，2355 行新增 / 1091 行删除  
**总体评估**: APPROVE (all items resolved)

---

## 修复进度

| 级别 | 总数 | ✅ 已修复 | ❌ 未修复 |
|------|------|----------|----------|
| P0 | 2 | **2** | 0 |
| P1 | 3 | **3** | 0 |
| P2 | 3 | **3** | 0 |
| P3 | 2 | **2** | 0 |
| **合计** | **10** | **10** | **0** |

---

## 改动概览

| 模块 | 文件数 | 新增/删除 | 主要改动 |
|------|--------|-----------|----------|
| `aemeath-core` | 16 | +723/-180 | Task 系统重构、配置合并修复、Scheduler 重试修复、并发工具 join_all |
| `aemeath-llm` | 4 | +116/-221 | Provider 重构、stream 超时检测、5xx 重试、消息序列修复 |
| `aemeath-tools` | 15 | +695/-184 | Edit 模糊匹配、Bash 安全加固、Read 路径穿越防护、TodoWrite 异步修复 |
| `aemeath-cli` | 13 | +577/-340 | TUI async 重构、消息同步、权限门控、UI 优化 |
| 其他 | 4 | +244/-166 | Provider 定义、env 配置、Cargo.toml 依赖 |

---

## P0 — 严重（全部已修复）

### 1. API Key 明文泄露 — ✅ 已修复
**文件**: `env`  
**修复**: `env` 文件已从仓库中删除

### 2. Shell 注入检测误杀合法命令 — ✅ 已修复
**文件**: `aemeath-tools/src/bash.rs`  
**修复**: 
- `check_shell_injection` 改为子命令级分析，`is_safe_chain_command` 允许 `cd && readonly_cmd`
- `allow_all` 时通过 `ToolContext.allow_all` 跳过注入检查

---

## P1 — 高

### 3. TUI 权限门控丢弃图片数据 — ✅ 已修复
**文件**: `aemeath-cli/src/tui/app.rs`  
**修复**: 用 `tool_name_map` 通过 id 查找工具名称，保留 `images` 字段在 `all_results` 中

### 4. 并发工具结果顺序 — ✅ 已修复
**文件**: `aemeath-core/src/agent.rs`  
**修复**: 用 `Vec<Option<ToolResultTuple>>` + position tracking，按原始 tool_calls 顺序返回结果

### 5. 模糊匹配丢失 trailing newline — ✅ 已修复
**文件**: `aemeath-tools/src/file_edit.rs`  
**修复**: `fuzzy_find_in_content` 检查 `old_string.ends_with('\n')` 并保留

---

## P2 — 中

### 6. 配置合并无法区分"显式设置为默认值" — ✅ 已修复
**文件**: `aemeath-core/src/config.rs`  
**修复**: `provider` 改为 `Option<Provider>`，merge 用 `.or()`

### 7. Scheduler 嵌套锁序风险 — ✅ 已修复
**文件**: `aemeath-core/src/scheduler.rs`  
**修复**: 文件头部添加了锁序文档注释

### 8. catch_unwind 后 UI 残缺无提示 — ✅ 已修复
**文件**: `aemeath-cli/src/tui/app.rs`  
**修复**: `catch_unwind` 失败时调用 `status_bar.set_warning("Render error, try resizing")`

---

## P3 — 低（全部已修复）

### 9. `/save` 命令不必要的磁盘 I/O — ✅ 已修复
**文件**: `aemeath-cli/src/repl.rs`  
**修复**: `resumed_session` 作为参数传入 `handle_slash_command`，直接复用内存中的 session 对象

### 10. `as_millis() as u64` 强转 — ✅ 已修复
**文件**: `aemeath-core/src/task.rs`  
**修复**: 改为 `.try_into().unwrap_or_default()`

---

## 本次改动的亮点

| 改进 | 说明 |
|------|------|
| TUI async 重构 | 去掉 `block_in_place`/`block_on`，改为纯 async + `EventStream`，消除死锁风险 |
| 并发工具执行 | `join_all` 替代串行 for 循环，真正并行，结果按原始顺序返回 |
| TaskStore 自锁修复 | `create()` 中 `next_id` 锁缩小到 `{}` 块内 |
| Edit 模糊匹配 | 忽略缩进差异 + 自动适配缩进 + 保留 trailing newline |
| Bash 安全加固 | 子命令级链式分析 + `allow_all` 旁路 + 重定向检查 |
| 文件路径穿越防护 | `validate_and_normalize_path` + workspace 边界检查 |
| 消息历史回传 | `MessagesSync` 事件解决 TUI "答非所问" |
| Cancel 消息回退 | `messages.truncate(messages_at_start)` 完整回退到本轮开始前 |
| 配置合并 | `Option<Provider>` 区分"未设置"和"显式设置" |
| Scheduler 修复 | 重试逻辑修复 + 锁序文档化 |
| Session 元数据保留 | 恢复会话后保存时保留原始 `created_at`/`metadata` |

---

## 残留项（0 项）

所有 review 问题已全部修复。

---

## CR 后追加修复

以下问题在 CR report 之后发现并修复：

| # | 问题 | 修复 |
|---|------|------|
| 11 | `allow_all` 时 Bash 链式命令被误杀 | `ToolContext` 新增 `allow_all` 字段，BashTool 在 `allow_all` 时跳过 `check_shell_injection` |
| 12 | Cancel 后消息历史污染下一轮对话 | `messages.truncate(messages_at_start)` 完整回退到本轮开始前，覆盖所有 cancel 路径 |
| 13 | 滚动位置被 streaming 文本打断 | streaming 重渲染行数减少时 `scroll_offset` 同步 `saturating_sub`；trailing newline 在 offset 调整前计入 |
| 14 | TodoWrite 结果重复显示 | Task 工具（TodoWrite/TaskCreate/TaskUpdate/TaskList）跳过 `│` 内容行，只显示 `└─ ✓ completed` |

---

## 审查覆盖范围

| 已覆盖 | 未覆盖 |
|--------|--------|
| 安全（注入、路径穿越、API key） | 性能基准测试 |
| 并发正确性（死锁、锁序、竞态） | 跨 provider 兼容性测试 |
| 错误处理（panic、超时、重试） | MCP 协议合规性 |
| 消息历史完整性（cancel 回退） | 大文件/长对话压力测试 |
| SOLID 原则（SRP、DIP） | 国际化/多语言 |
