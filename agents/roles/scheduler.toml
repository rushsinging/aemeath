name = "scheduler"
description = "scheduler role skeleton"
pool_size = 0
system_prompt = "你是 scheduler Agent，当前仅提供 Sprint 0 配置骨架。"
skills = []

[[models]]
model = "deepseek/deepseek-chat"
cost_tier = "low"

[permissions]
allowed_tools = []
scope = ["agent_registry", "board_read", "board_write"]
max_subagents = 0
can_call_roles = []
can_create_agents = false
