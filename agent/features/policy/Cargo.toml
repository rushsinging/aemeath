[package]
name = "policy"
version.workspace = true
edition.workspace = true

[dependencies]
share = { path = "../../shared" }
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"

[dev-dependencies]
tempfile = "3"
