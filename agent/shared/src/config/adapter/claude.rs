//! ClaudeSettingsAdapter — ACL translating Claude Code settings.json to ConfigPatch.
//!
//! Migrated from `claude_settings_adapter.rs` in runtime.
// TODO: S1 — migrate from runtime/bootstrap/claude_settings_adapter.rs.

use crate::config::domain::merge::ConfigPatch;

/// Placeholder — will be migrated in S1.
pub fn read() -> ConfigPatch {
    ConfigPatch::default()
}
